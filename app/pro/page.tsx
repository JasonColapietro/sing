import { ProClient } from "@/components/pro/pro-client";

export const metadata = {
  title: "Pro",
  description:
    "Unlock every song, every warmup tier, and unlimited saved takes — one $9 purchase, no subscription, no account.",
};

export default function ProPage() {
  return <ProClient />;
}
