# Ship loop continuation — 2026-09-23

Follows `2026-09-05-practice-parity.md`. This session started in a fresh cloud
container. The loop ledger, the three halted graph-flo worktrees
(`ship-0a572684…`, `ship-1d405dd2…`, `ship-8e008674…`) and the reviewed S0a
plan from the earlier session were not present, so this file is the only
written record of the loop's state. Slices S2–S8 and S10 are not described here
because their scopes lived only in that ledger; re-derive them before running
them. (They have since been re-derived and run; see Shipped and Queue.)

## Shipped

| Slice | PR | What |
|---|---|---|
| S0b | #148 | Competitor exercise-mechanics refresh in `docs/research/competitor-parity-2026-09-04.md`: 23 sourced rows: 6 ✓ / 5 ◐ / 10 ✗ / 2 out of scope (published outcome data is ✗, pending measured data from #141). |
| S0a | #149 | Pitch-precision harness, unit and in-browser (below). |
| S1 | #152 | Pitch engine: true median in the smoothing, 4 kHz pre-filter in the detector (below). |
| S2a | #155 | Songs: free A–B loop handles that snap to note starts, sections as presets. Scoring, judging and loop closing moved onto the lagged pitch clock, so short loops score fairly (`e2e/song-loop-scoring.mjs`). |
| S2b | #157 | Songs: record your take from the scored mic stream; play it back or save it to Recordings from the summary. |
| S3 | #159 | Songs: skippable onboarding taste quiz (styles, experience, goal) driving "Picked for you"; locked and Pro songs are dropped first, range fit outranks taste. |
| S4 | #161 | Ear: Note Catcher, a pitch-steered catching game with "Chase the notes of <song>"; added to Pitch lab (526 s → 570 s). |
| S5a + S5b (sing side) | #154, #156, #158, #160, #162, #165 | Voice curriculum on sing: the catalog contract, lesson routes and all 102 lessons, then the cut-over (#165): `lessonBaseUrl` on sing, `migration.phase` "hosted", a URL map for every lesson in `contracts/suede-voice-lesson-urls.json`, and the footer and home page linking /learn/voice. Built by the local session; its own notes belong in the S5b section below. GuitarHub's side merged as suede-guitar-hub#48 (redirects from that URL map, web voice track retired). |
| S6 | #163 | Warmups: "Today's three", three free exercises a day from recent scores with a tempo step each; stable all day, rolls over at midnight. |
| S7 | #164 | In-app "Your week" report on the first visit of a new week (no email or push setup exists); shares week boundaries with /progress. |
| S9b | #171 | Warmups: vibrato drill (rate and width read after each hold against 5–7 Hz; the tail of the hold is re-read so a straight start doesn't hide it), fry onset with an unscored creak lead, recovery, high-note and Pro mix drills, a routine for each. `suede-vocal` v4 marks `vibratoRateHz` measurable (`npm run e2e:vibrato`). |
| S9a | #176 | `/programs`: five named programs (7 days to 6 weeks, 10–20 min a day), a Today card deep-linking into each room, day completion derived from logged sessions (each session counted once, one day per calendar day, restarts cut off by start time). `suede-vocal` v5 (`npm run e2e:programs`). The local session's #180 then added the book's twelve-week plan and made week 1 of every Pro program free (v6). |
| S9c | #178 | Breath: the mic hears the inhale (`lib/audio/breath-detect.ts`); the sustain test and a new "Breathe and sing" drill arm on a heard breath, with a fallback to start without it. A breath that runs straight into a hiss is split at the step up, so the hiss is timed. `suede-vocal` v7 records inhale as heard, not measured (`npm run e2e:breath`). |
| Fix | #181 | Warm-up ladders keep patterns narrower than an octave under the saved high in a tight range (#171 did it for wider ones). Song-first practice is unchanged. |
| iOS upkeep | Suede-AI/suede-voice#136, #137 | Attribution tests wait on outcomes instead of yield counts (a flaky retry test failed once on #135); the vendored `suede-vocal.json` re-synced to v7. |

## Decisions

- **Engine.** graph-flo halted three times on S0, each time before writing any
  code: task wording that asked for browser tests, a one-comma quote mismatch,
  and the 5-lane cap at standard budget plus an unrecognised plan source. The
  user chose to build slices directly: lint, unit tests, build and the
  browser precision run, then merge. graph-flo was kept for the risky
  slices, S1 and S5b; see the next point for S1.
- **Stop rule.** A slice lineage that halts three times stops the loop. The
  choice then goes back to the user instead of another retry.
- **Merge autonomy.** Slices merge on their own once CI is green (as #148
  and #149 did).
- **S1 and S5b engine.** The user chose to build S1 and S5b directly as well,
  so no slice currently uses graph-flo.

## Pitch-precision harness (S0a)

- `lib/audio/voice-fixture.ts`: a deterministic synthetic voice (harmonics,
  detune, vibrato, jitter, noise) plus a WAV encoder, shared by both layers
  below.
- `lib/audio/pitch-precision.test.ts`: C2–C6 at 44.1/48 kHz. Ceilings are
  measured values with headroom, and each figure is recorded in the file
  header.
- `npm run e2e:pitch` (`e2e/pitch-precision.mjs`): runs 12 cases through
  Chrome's fake microphone into `/studio`. It reads what `usePitch` heard via
  `window.__singPitchProbe`, a hook that does nothing unless the harness
  creates it. Usage and measured values are in `e2e/README.md`.

Measured: clean notes read within 0.2 c with no octave errors. At 10 dB SNR the
error reaches p95 16 c and max 31 c, and 2 of 294 frames are missed.

## S1 (pitch engine), built directly

- **Median bias fixed.** `lib/audio/median.ts` averages the two middle values.
  It's used by `usePitch`, ear-game note segmentation and the
  spectral-envelope F0, which all took the upper middle value before.
  Browser vibrato bias went from 5.5 c sharp to 0.7 c (ceiling 1.5 c).
- **Noise weak spot fixed.** `detectPitch` low-passes each frame at 4 kHz
  before the NSDF. At 10 dB SNR, p95 went from 16 c to 1.3 c; at 5 dB SNR,
  misses went from 83/294 to 0. Clean frames cost ~0.2 c (max 0.4 c,
  0.7 c at 2048 samples). All existing noise-only, room-tone and mains-hum
  rejection tests pass unchanged.
- **Scoring impact.** Every room now reads a vibrato ~5 c flatter than before,
  i.e. centred. Scores for singers with wide vibrato may move slightly.

## S5b: GuitarHub cut-over (shipped)

### What shipped (2026-09-24)

| PR | What |
|---|---|
| #154 | S5a's catalog (`contracts/suede-voice-curriculum.json`, `lib/voice-curriculum.ts`) re-applied from draft #143 on main, with its curriculum refreshed to GuitarHub #47's `voice.json`. #143's `/learn` page was dropped; main's #150 hub stays. `e2e/run.mjs` gained `--executable`. |
| #156 | `content/lessons/<stage>/<module>/<lesson>.md` → `scripts/compile-lessons.mjs` → `lib/lesson-data.ts`. `scripts/port-voice-lessons.mjs` ports GuitarHub's `voice-instruction.json` verbatim apart from listed UI substitutions. Routes `/learn/voice`, `/[stage]`, `/[module]`, `/[lesson]` (static, canonical, in the sitemap). `lib/lesson-practice.ts` ports GuitarHub's per-module proof table and material to sing's own rooms, songs and chapters. `lib/voice-lessons.test.tsx` is the honest-claims and deep-link test. The Learn tab, `/learn`, `llms.txt` and e2e `ROUTES`/`discoverTemplateRoutes` are wired in. Stages 1–2. |
| #158 | Stages 3–4. The twelve "Checkpoint:" lessons nothing measures became "Self-Check:" in the catalog. Also fixes Codex findings from #154 and #156: empty catalog arrays, and a failed compile overwriting generated data. |
| #160 | Stage 5. A song lesson with no song now uses its supplied pattern, and objectives are held to the catalog summaries. |
| #162 | Stages 6–7. The held-harmony self-check is reworded as practice, the three-note module gets a three-note exercise, and the blues call is pitched from the heard tonic. |
| #165 | Cut-over. The contract records sing as lesson host, `phase: "hosted"`, redirects enabled, and a `migration.resolution` line for every piece of required evidence. New generated `contracts/suede-voice-lesson-urls.json`. `suede-progress` records the new ownership. The footer links `/learn/voice`. |
| suede-guitar-hub#48 | **Merged 2026-09-24 with the owner's approval.** Builds on and supersedes GuitarHub #42. 308 redirects for `/learn/voice` and all 102 lesson IDs, from the vendored URL map, with no catch-all. Two pages stay on GuitarHub on purpose: `/learn/voice/materials`, the paid lifetime practice library that sing has no equivalent of, and a new `/learn/voice/recordings`, where takes saved in GuitarHub's IndexedDB can still be played or deleted (sing can't read them). Retires the voice track from GuitarHub's web routes, sitemap and links. Keeps `voice.json` and the instruction data for the native learning contract. Re-vendors sing's contracts, including `suede-vocal.json`, which GuitarHub main was already stale against after sing #161. |

### Decisions made along the way
- **Every stage is free on sing** (owner, 2026-09-23). GuitarHub charged for stages 3–7; nothing is gated on sing.
- **Release checks.** On 2026-09-24 the owner reported that the qualified vocal review and the physical-microphone checks were done. The contract records them as done on the owner's report, dated, and says the records are not stored in either repository.
- **No lesson completion is tracked.** Module and lesson pages show practice history: a count of the module's room sessions on this device, with the room's best score. The page says the count is not a pass for the lesson. Recording completion would need a `lesson` activity type in `lib/progress-shape.ts` and the `suede-progress` contract, which waits for the owner's approval.
- **Catalog divergence.** sing's catalog now differs from GuitarHub's `voice.json` in text (self-check retitles, reworded summaries, free access). Parity with GuitarHub is by ID.
- **Tolerance.** Lesson pages state sing's `TOLERANCE_CENTS` (50 c), not GuitarHub's 35 c.

### Still open

suede-guitar-hub#48 merged on 2026-09-24, and GuitarHub's contract checks pass against sing's main. The superseded drafts, sing #143 and suede-guitar-hub #42, are closed.

- **Human checks from #143 not done here:** a browser/visual pass by a person, keyboard-only navigation, and a screen-reader pass of the `/learn/voice` pages. The automated mobile audit (`e2e:mobile`) reports 0 blockers and 0 majors; that isn't a substitute.
- **iOS / GuitarHubCore** still bundles the voice track. Retiring it there is outside this slice.
- **GuitarHub's `lib/learning-sync/sing-sessions.ts`** still maps sing sessions to voice lesson IDs for GuitarHub's own ledger. It was left for existing history.

### Original brief (2026-09-23)

sing becomes the only host of the voice curriculum (7 stages, 34 modules,
102 lessons, currently at guitarhub.org/learn/voice). The user chose to
build it directly, not through graph-flo.

**Why it moved to a local session.** The cloud container's network policy
denies guitarhub.org and sing.suedeai.ai. The user also declined attaching
`suede-guitar-hub` to the cloud session. So the lesson text wasn't reachable,
and none of it is in this repo. Don't write lessons from memory: port them
from the source.

**Start from S5a, draft PR #143.** "Publish voice curriculum ownership and
discovery hub" (branch `codex/voice-curriculum-ownership`, 2026-09-14) is
unmerged and now conflicts with main. It already adds:
- `contracts/suede-voice-curriculum.json`, a versioned 7/34/102 catalog that
  is byte-identical to GuitarHub's
- `lib/voice-curriculum.ts` and its tests
- a `/learn` hub, which main has since replaced with the #150 authority hub
- the sitemap and footer changes
- a design spec and plan under `docs/superpowers/`

It was built for the earlier decision, where sing owned the catalog and
GuitarHub stayed the lesson host with no redirects. The 2026-09-23 decision
supersedes that: sing hosts the lessons. Reuse #143's catalog and module,
merge it with main's `/learn`, then build S5b on top of it. Its companion is
suede-guitar-hub #42, whose check fails until the catalog is on sing's main.
#143 also lists open checks: browser/visual, keyboard, screen reader,
physical microphone, and qualified vocal review.

**Source of truth for lesson bodies.** The curriculum lives in the GuitarHub repo
(`suede-guitar-hub`; `contracts/suede-progress.ts` names it
`JasonColapietro/suede-guitar-hub`). The PRs about it are sing #143 and
suede-guitar-hub #37, #42 and #47. Fall back to the live pages only if the
repo can't be read.

**Design (reuses what sing already has):**

- **Content.** Put lessons in `content/lessons/<stage>/<module>/<lesson>.md`
  with frontmatter, compiled by a new `scripts/compile-lessons.mjs` into a
  generated `lib/lesson-data.ts`. The book does the same with
  `content/book` → `scripts/compile-book.mjs` → `lib/book-data.ts`.
- **Routes.** `/learn/voice/[stage]/[module]/[lesson]`, plus stage and module
  index pages. Follow `app/book/[slug]/page.tsx`: `dynamicParams = false`,
  `generateStaticParams`, and `withCanonicalOpenGraph` with a canonical under
  `SITE_URL` and OG images (`lib/og.test.ts`). Add the pages to
  `app/sitemap.ts`, but never a noindex one (`lib/sitemap.test.ts`). Any FAQ
  needs visible headings (`lib/seo-section-structure.test.tsx`).
- **Entry points.** Add `/learn/voice` to the Learn tab's `also` list in
  `components/nav.tsx`, and link it from `/learn` (`VOCAL_LEARNING_PATHS`),
  `lib/llms-txt.ts` and the landing page. The footer's
  `MORE_FROM_SUEDE` "Voice curriculum on GuitarHub" link becomes sing's own
  page. Update the test that pins that anchor,
  `lib/internal-linking.test.tsx:156-166`.
- **Honest claims.** A lesson may only promise proof that a
  `measurable: "yes"` row in `contracts/suede-vocal.ts` supplies; everything
  else is worded as practice. Enforce it with a test modelled on
  `lib/programme-exit-tests.test.tsx`. The contract's `UNSUPPORTED_CLAIMS`
  already names claims the GuitarHub lessons make that sing can't measure
  (flow consistency, personal passaggio from a range scan, strain-free
  verdict, vibrato rate on cue, held harmony against a lead, pitch
  steadiness in the breath room). Those lessons get reworded, not copied.
  The GuitarHub lessons use a 35 c tolerance while sing uses
  `TOLERANCE_CENTS`; state which one applies.
- **Deep links.** Use only `contracts/suede-vocal.ts` `deepLinks` rooms and
  parameters (`/warmups?exercise=|routine=`, `/breath?drill=|routine=`,
  `/songs?song=`, the rest bare). Never deep-link a Pro exercise from a free
  page. Links on sing itself are relative, not `https://sing.suedeai.ai`.
- **Completion.** Start with completion derived from existing room
  sessions (`lib/progress.ts` `SessionLog`), like the stars
  `components/practice/learn-home.tsx` computes for `PathList`. A new
  `lesson` activity type would change `lib/progress-shape.ts`, its
  validators and the `suede-progress` contract, so it waits for the user's
  approval.
- **Contracts.** Once sing hosts the lessons, rewrite
  `contracts/README.md` and `contracts/suede-progress.ts` (lesson-ID
  ownership), then regenerate with `CONTRACT_WRITE=1 npx vitest run
  contracts/`.
- **GuitarHub side.** Redirect guitarhub.org/learn/voice/** to the matching
  sing URLs and retire GuitarHub's copy, in the GuitarHub repo. That's a
  separate PR there, which the user must approve.

**Done means:** all 102 lessons ported and reachable, unsupported claims
reworded, tests (`npm run lint`, `npx tsc --noEmit`, `npm test`), `npm run
build`, `npm run e2e:mobile` covering the learn pages, and the PRs merged
once CI is green. The e2e audit doesn't visit any learn page today:
`e2e/routes.mjs` has no `/learn` in `ROUTES`, and `discoverTemplateRoutes`
only picks singer, song, book and atlas pages. So the scaffold PR adds
`/learn` and `/learn/voice` to `ROUTES`, plus stage, module and lesson picks
to `discoverTemplateRoutes`. Split it into several PRs: routes and scaffold first, then
the content in stage batches, then the cut-over.

## S8: iOS part 1 (2026-09-24)

Merged: [Suede-AI/suede-voice#132](https://github.com/Suede-AI/suede-voice/pull/132)
into `sing/v1.0` as `6e2f0b6`, with iOS, Android and privacy CI green. It wasn't
submitted anywhere, and the App Store metadata and release train are unchanged.
This cloud session attached suede-voice directly. That supersedes the
local-session card "Build iOS song scoring in suede-voice (S8)": don't start it.

What the iOS song room does now:

- **Scores from the microphone.** `NSDFPitchDetector` ports `lib/audio/pitch.ts`
  (NSDF, 4 kHz pre-filter). `SongPitchSmoother` is the true median of 4 readings
  with a 0.75 clarity gate. A 4,096-sample frame is read 60 times a second.
- **Scores on the lagged clock.** `PitchLatency` follows `lib/audio/latency.ts`
  and adds iOS input latency. Crediting, judging and pass closing all run on
  the lagged position, with `TOLERANCE_CENTS` and `MIN_VOLUME` asserted against
  `practice-parity.json`. Readings are placed at their capture time rather than
  "now", because tap blocks arrive about every 100 ms.
- **Has the note highway and the three passes.** The highway ports
  `piano-roll.tsx`. The passes are Listen, Sing along and On your own, with Sing
  along as the default. Listen never opens the microphone.
- **Cancels echo by route.** It uses voice processing on the engine that plays
  the guide, and only when the guide plays into an open speaker. A route change
  mid-run that needs the canceller pauses the run.
- **Follows both contracts.** `suede-vocal.json` is now vendored in
  suede-voice, and its `pitch` section is asserted. `NSDFPitchDetectorTests`
  checks the port against `detectPitch` readings on identical frames.

Not verified: **anything on a physical device.** Acoustic parity is not claimed.
The checklist is in suede-voice `docs/handoffs/2026-09-24-s8-song-scoring.md`:

- speaker with echo cancellation (a silent singer must score about 0)
- headphones, AirPods and Bluetooth latency
- a quiet voice with voice processing on
- onset alignment at both tempos
- interruptions and route changes
- guide ducking
- CPU

### S8 follow-ups (2026-09-24)

All merged with CI green and no open review findings. Nothing was submitted to
the App Store.

| PR | What |
|---|---|
| [#172](https://github.com/JasonColapietro/sing/pull/172) | `songTempo` in `contracts/practice-parity.ts` and `contracts/README.md` no longer says native songs lack a microphone. The gap is the web's tempo grid and auto-tempo, which have no native counterpart yet. It named no slice for them, because S10 was unconfirmed at the time. Wording only; version stays 2. |
| [#173](https://github.com/JasonColapietro/sing/pull/173) | `suede-vocal.json` v3 adds `pitch.engine`: pre-filter corner, silence floor, edge trim and minimum window, peak tolerance, mains-hum check, frame size, clarity gate, and the true median of 4. The values come from named constants. `usePitch` and `useAnalyser` no longer use literals, and a test checks the sources still use the constants. |
| [Suede-AI/suede-voice#134](https://github.com/Suede-AI/suede-voice/pull/134) | Re-syncs both contracts byte-identically. `SuedeVocalContractTests` asserts every `pitch.engine` key against the Swift constant that mirrors it, so a retune here fails there by name. suede-voice docs no longer treat S10 as settled scope. |
| [Suede-AI/suede-voice#133](https://github.com/Suede-AI/suede-voice/pull/133) | The iOS microphone prompt now reads "...to measure your range and register, and to score the songs you sing." A test checks the shipped `NSMicrophoneUsageDescription` equals the onboarding copy. The owner approved the merge because App Review sees the string. It ships with the next build, not the 1.6 build in review. Android keeps its wording; it has no songs. |

Still open:

- **On-device verification of S8.** Nothing has been tested on a physical
  iPhone, so acoustic parity is still not claimed. The checklist is above and
  in suede-voice `docs/handoffs/2026-09-24-s8-song-scoring.md`.
- **Release.** Refresh the App Store state before any release work. The 1.6
  submission was in review, and the new microphone prompt rides the next
  build.
- **Attribution footers.** Three review-thread replies on suede-voice #132 carry
  a "Generated by Claude Code" footer that the GitHub tooling added. They can
  only be edited on GitHub.

S10 (XP, stars, daily goal, song modes, auto tempo and mastery) was confirmed
and drafted on 2026-09-24; see the S9 and S10 open items below. As of S8 alone,
scored song runs were not saved to history; the S10 draft stores them in their
own table.

## S9 and S10 open items (2026-09-24)

- **S10 is not shipped.** It is drafted as Suede-AI/suede-voice#135: iOS
  songs get 5-star grades, XP, daily goal and streak from song runs, a section
  loop, guide level, auto tempo and mastery, all on the web's numbers. It was
  written without a Swift toolchain; CI compiles it and the suite is green. A
  review pass fixed Home's streak ignoring songs, a doubled streak on
  Progress, and a tempo tap during finish costing mastery, and added a v3→v4
  migration test. A local session is building a second S10, so only one of
  the two should merge.
- **Breath detection** is measured on synthetic breaths only. Quiet nasal
  breaths and some mics will be missed, which is why the fallback exists.
- **S10 on a device.** Pick one of the two S10 versions to merge. Then check:
  the count-in at low guide levels (the level is the engine's output volume),
  the guide level across voice-processing restarts, the song room at small
  widths, that Loop being on by default doesn't hide how to earn mastery, and
  that Home's two streak labels (all sessions vs practice) read clearly.

## Queue

S2–S8 and S10 were reconstructed on 2026-09-23 from the parity doc, the
handoffs and PR history. The loop's ledger was lost, so apart from S5a these
are inferences, not recoveries. **The user confirmed S2–S8 and their order
on 2026-09-23**, so they run as listed. The user confirmed S9 and S10 on 2026-09-24.

| Slice | Proposed scope | Confidence | Status |
|---|---|---|---|
| S2 | Songs: loop any section with sliders (today a fixed picker), plus record and review inside the song (parity rows ◐/✗) | Medium | Shipped: #155, #157 |
| S3 | Onboarding quiz (genres, favourite singers) feeding song picks | Low | Shipped: #159 (no favourite-singers question; the singer directory doesn't map onto the public-domain songbook) |
| S4 | Path games: Note Catcher, and Chase the Notes tied to the path's songs | Low | Shipped: #161 |
| S5a | Voice-curriculum catalog and discovery: draft PR #143 | High | Shipped by the local session as #154; S5b's lessons shipped in #156–#162 and sing's side of the cut-over in #165. GuitarHub's redirects and retirement shipped in suede-guitar-hub#48; #143 closed |
| S6 | Three personalised daily exercises, Vanido-style, with difficulty adapting to performance (extends #130's routine picker) | Medium-low | Shipped: #163 |
| S7 | Weekly report as a Monday email or notification; the /progress card exists | Low | Shipped in-app: #164. Email or push still needs a provider |
| S8 | iOS (suede-voice `sing/v1.0`) part 1: microphone-backed song scoring, note highway, sing-along by default | Medium | Merged in suede-voice #132 (2026-09-24); not yet verified on a device, see the S8 section above. The local-session card is superseded |
| S9 | Multi-week programs, plus vocal-fry, vibrato, mix-voice and high-notes drills, and breath detection | Medium | Shipped: #171 (drills), #176 (programs), #178 (breath) |
| S10 | iOS part 2: XP, stars and daily goal, song modes, auto tempo, mastery. Alternative: publish outcome data (#141 primitives, synthetic-only so far) | Low | Confirmed by the user on 2026-09-24 and drafted as Suede-AI/suede-voice#135 (CI green, not device-verified); a local session is building another version. Outcome data stays unshipped: only synthetic data exists |

S9's scope matches the first refresh item exactly, so S9 may itself be
post-refresh, replacing an original S9. Parity gaps no slice covers yet:
published outcome data (unless S10 takes it), video courses, iOS setlists
and achievements. Parity rows 43, 44, 45, 53 and part of 60 were closed by
#130 but still read ✗/◐ in the matrix.

- S9: multi-week programs, plus vocal-fry, vibrato, mix-voice and high-notes
  drills, and breath detection.
- S11: harmony trainer.
- S12: solfège with lives.
- S13: custom exercise editor, with key isolation and cross-key scoring.
- S14: MIDI melody sing-along.
- S15: pitch-accuracy test and accuracy-coloured range bar.

## Environment notes

- In the cloud container, Playwright's pinned Chromium isn't installed. Run
  `npm run e2e:pitch -- --executable=/opt/pw-browsers/chromium`. Locally the
  run defaults to `channel: "chrome"`, like `e2e/run.mjs`.
- The e2e runner imports the TypeScript fixture directly, which needs
  Node 22.18 or newer.
- Don't `pkill -f "next start"` from a Bash tool call: the pattern matches the
  calling shell's own command line and kills it.
