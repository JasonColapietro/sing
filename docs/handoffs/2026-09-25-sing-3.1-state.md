# Handoff: Suede Sing 3.1, current state (2026-09-25)

This is the entry point for the next session. `2026-09-23-ship-loop.md` has the
slice-by-slice history (S0a to S10) behind this summary.

## Release: Suede Sing 3.1

The owner named the September wave **3.1**, skipping 3.0. It ships with the PR
that adds this file:

- **`/changelog`** now leads with six 3.1 stories: lessons, programs, songs,
  breath and vibrato measurement, the pitch engine, and daily practice. The v2
  stories stay underneath as "Earlier: v2, August 2026". The running log gains a
  September 2026 month with 21 entries, dated by merge day in US Eastern time. The 3.1 entry is the 24th: #187 merged at 20:44 ET.
- **The site-wide banner** (`components/v2-banner.tsx`) now announces 3.1 with
  the copy "102 voice lessons, multi-week programs, and songs that score you."
  `SEEN_KEY` went from `suede-sing:v2-banner:v1` to `suede-sing:v3-1-banner:v1`,
  so people who dismissed the v2 banner see it once more. The file, component
  and DOM id keep their v2 names, which are internal only.
- **The footer link** reads "3.1: what we've changed".

The owner asked for changelog copy that is **technical and never
unflattering**. Every figure in it was checked against the code or the PR that
shipped it:

- 7 stages, 34 modules and 102 lessons, from `contracts/suede-voice-curriculum.json`
- 6 programs, 4 free, with Pro programs' `freeWeeks: 1`, from `contracts/suede-vocal.json`
- vibrato bias from 5.5 c to 0.7 c (#152)
- tempo 25–125% in 5% steps (#129)
- inhale detection with a 2048-point FFT over 0.25–2.5 s (#178)
- a vibrato target band of 5–7 Hz (#171)
- Note Catcher's 0.4 s hold (#161)

Fixes, caps and caveats are left out of the public copy on purpose. When you
add entries, keep this style: one line each, what a singer can use, newest
first. Don't give numbers the code doesn't hold, and keep em dashes out of the
title and meta (#132).

## State by surface

| Surface | State |
|---|---|
| sing (web) | `main` has 3.1. All S2–S9 slices are shipped. 1,134 of 1,139 unit tests pass and 5 are skipped. Under load, up to 8 tests in `lib/audio/pitch-precision.test.ts` (and sometimes the lesson render or narrow-range warmup tests) time out at 5 s, but they pass when run alone. |
| Suede Voice iOS | **1.8 (25) is `VALID` in TestFlight** (suede-voice #140). It hasn't been submitted for App Review. 1.7 (24) is `READY_FOR_SALE`. 1.8 adds microphone-scored songs (#132), stars, XP and mastery (#135), and the 26-song songbook, all free (#139). |
| Suede Voice iOS, merged after build 25 | **suede-voice #142**: loops snap to note starts (pickup songs no longer loop from mid-word), two-part songs play once by default, song runs count toward the 7-day-streak achievement, the copy no longer says "phrases" or "full arrangements", and `songTempo.ios` is re-synced. It isn't in build 25; **cut build 26 before submitting**, or at least fix the release-notes text in App Store Connect. |
| Suede Voice iOS, next | Draft **suede-voice #141** has the audio-reliability port, with 921 simulator tests passing. It waits on the six-item device checklist in the PR. It isn't in 1.8. |
| GuitarHub web | The voice track redirects to sing (#48). Contracts are re-synced to v7 (#49). |
| Contracts | sing, suede-voice and GuitarHub are byte-identical for every vendored contract as of 2026-09-24. |

## Adversarial pass (2026-09-24, late)

Four adversarial reviewers checked the 3.1 copy, the handoffs, iOS 1.8 and
draft #141:

- **Site (#188):** nine 3.1 changelog claims are made exact. The Pro page, its
  JSON-LD, the Pro hero and the song-summary nudge no longer sell "the full
  songbook", which has been free since `PRO_SONGS` emptied, and now name
  scoring history. The two-part songs are "longer versions". The handoffs
  and 11 parity rows are corrected.
- **iOS (#142):** see the table above.
- **#141 (draft):** the busy deactivation (isBusy) no longer replays onto
  another owner, and a stuck cleanup can no longer lock out the mic. The fix
  also covers the timeline gap after a rebuild, resume after a failed start
  during an interruption, and idle deinit. 931 tests pass. The device checklist
  still applies.

## QA pass (2026-09-25)

- **Production (`sing.suedeai.ai`):**
  - All 899 sitemap URLs return 200, including the 143 `/learn/voice` pages.
  - The 3.1 copy, the banner, the Pro scoring-history wording and the "longer
    version" labels are live.
  - `/learn` and `/analyze` load with no console errors after #190.
- **GuitarHub:**
  - `/learn/voice` and all 102 lesson IDs 308 to the exact sing lesson URL.
  - `/learn/voice/materials` and `/learn/voice/recordings` still return 200.
- **Full UI/UX audit** (`node e2e/run.mjs`, 9 audits × 36 routes × 5
  viewports, against a local production build):
  - Result: 0 blockers, 8 majors, 232 minors. None of the majors came from 3.1.
  - #190 fixes all 8:
    - `/analyze` hit a React #418 hydration error every day after a deploy,
      because the prerendered weekday labels went stale.
    - `/privacy` and `/terms` redirect off-site, so their prefetch failed CORS.
    - The lesson and stage prev/next links were 20px targets.
  - Re-audit of the affected routes: 0 blockers, 0 majors.
  - The 232 minors are unchanged. They include no dark mode, 44px-comfort tap
    targets, the `<select>` border contrast (1.41:1) on /warmups, and the footer
    paragraph's line length.
- **iOS `sing/v1.0`:**
  - SuedeVoiceTests: 879 pass.
  - SuedeVoiceUITests: 14 of 14 pass after #143. Three tests had been failing
    for harness reasons: the screenshot captures never answered the
    campaign-measurement sheet (#51), the Progress tile was below the fold, and
    a cold first launch was given 4 s.

## Open, in priority order

1. **Test 1.8 on a device before submitting it.** Build 25 is uploaded and `VALID`, but S8 (#132) and S10 (#135), its headline features, have never run on a physical iPhone. Run the S8 checklist in suede-voice `docs/handoffs/2026-09-24-s8-song-scoring.md` and the S10 checks in `2026-09-23-ship-loop.md` ("S9 and S10 open items"). After that, submitting is the owner's call.
2. **Device-test suede-voice #141**, then merge it for 1.8.1 or 1.9.
3. **Cleanup the owner has to approve.** The session's permission mode blocked
   deleting three things: the `Suede-AI--suede-voice.worktrees/s8` worktree, the
   `claude/s8-local` branch and the remote `claude/s8-ios-song-scoring`. #132
   already carries all of it. Also, `/Users/jason/code/JasonColapietro--sing` is
   a **suede-voice** worktree despite its name. It's on the Codex branch with 4
   modified files, all backed up as `backup/codex-native-parity-20260908`, so it
   can be removed once #141 settles.
4. **iOS parity gaps.** The web has these and the app doesn't:
   - programs and breath detection
   - the 102 lessons
   - Note Catcher
   - the taste quiz
   - today's three and the weekly report
   - song bands and breath marks. iOS has neither the data nor the UI; suede-voice #139's description wrongly says `breathMarks` was ported

   Each needs its own slice.
5. **From earlier handoffs, still open:**
   - the S7 weekly report by email or push, which needs a provider
   - a person's visual, keyboard and screen-reader pass of `/learn/voice` and `/programs`
   - GuitarHub's iOS app still bundling the voice track
6. **Carried over from older handoffs:**
   - `contracts/practice-parity.json` still gives `songTempo.ios` as `[0.75, 1]`
     with no auto tempo. Since suede-voice #135, iOS has auto tempo over
     `[0.5, 0.75, 1.0, 1.25]`. Regenerate it here, then re-vendor.
   - Web song mastery lives only on the device (`suede-sing:mastered:v2`) and
     isn't in account backup, yet the band ladder depends on it.
   - suede-voice `web.yml` doesn't check `toleranceCents` against the contract.
7. **Queue after S10:** S11 harmony trainer, S12 solfège with lives, S13 custom
   exercise editor, S14 MIDI melody sing-along, S15 pitch-accuracy test.

## Environment

- The iOS release build uses local ExportOptions: `destination: export` and the
  installed "Suede Sing AppStore" profile. The repo's `ExportOptions.plist`
  uploads on export and names a profile that isn't installed on the build Mac.
- The App Store Connect API key is in `~/.appstoreconnect`, and `altool` finds
  it by key ID. The app ID is 6767763231.
- The unpushed Codex work from 2026-09-08 is kept on suede-voice as
  `backup/codex-native-parity-20260908`.
